<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helpvest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EstudoActivity"></activity>
        <activity android:name=".FuvestProvasActivity" />
        <activity android:name=".UnicampProvasActivity" />
        <activity android:name=".ProvasActivity" />
        <activity android:name=".RedacaoActivity" />
        <activity android:name=".FuvestBibliotecaActivity" />
        <activity android:name=".UnicampBibliotecaActivity" />
        <activity android:name=".BibliotecaActivity" />
        <activity android:name=".EsqueceuSenhaActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
    </application>

</manifest>